# apps/landing: public marketing app.
# Copy this file to apps/landing/.env.local and fill in the values. Do not commit local env files.

# URLs. Leave blank when everything runs on one local/dev domain.
NEXT_PUBLIC_WWW_URL=https://www.example.com
NEXT_PUBLIC_APP_URL=https://app.example.com
