// Icon names from lucide-static. This is the source of truth for which icons
// the sprite includes — add a name here, run `npm run build:icons`, then use
// it as <Icon name="..." />. Names must match lucide-static/icons/<name>.svg.
export const lucideIconList = new Set([
  "alert-triangle",
  "check",
  "chevron-left",
  "chevron-right",
  "eye",
  "eye-off",
  "refresh-cw",
  "search",
  "users",
  "x",
]);
