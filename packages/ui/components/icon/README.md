# Icons

Icons are sourced from `lucide-static`. To add one, add its name to
`icon-list.mjs` and run `pnpm run build:icons`, then use it as `<Icon name="..." />`.

`sprite.svg` (in each app's `public/icons/`) and `icon-names.ts` are generated —
do not edit them by hand.
